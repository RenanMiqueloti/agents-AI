# Pré-commit hooks rodam local e no `pre-commit run --all-files` antes do PR.
# Os mesmos checks rodam no CI; pre-commit pega o problema antes do push.
#
# Setup:
#   pip install pre-commit
#   pre-commit install   # ativa o hook git pre-commit local
#
# Rodar manualmente em todos os arquivos:
#   pre-commit run --all-files

repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.6
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.13.0
    hooks:
      - id: mypy
        files: ^(agents/|api/|mcp_server\.py$)
        additional_dependencies:
          - pydantic>=2.0
          - types-requests
        args: [--ignore-missing-imports]

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-merge-conflict
      - id: check-added-large-files
        args: [--maxkb=512]
