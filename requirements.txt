# Interface web
streamlit>=1.35.0

# LangChain — núcleo e integrações (v1.x usa langchain.agents.create_agent)
langchain>=1.2.18
langchain-core>=1.4.0
langchain-community>=0.4.2
langchain-text-splitters>=1.1.2

# Providers — instale apenas o(s) que for usar
langchain-ollama>=0.2.0          # provider: ollama (local, gratuito)
langchain-anthropic>=0.3.0       # provider: claude  (requer ANTHROPIC_API_KEY)
langchain-openai>=1.2.1          # provider: openai  (requer OPENAI_API_KEY)

# LangGraph — agentes com ferramentas, HITL e checkpointing
langgraph>=1.2.4

# Vetores locais (RAG agent) + embeddings via sentence-transformers
faiss-cpu>=1.13.2
langchain-huggingface>=0.3.0
sentence-transformers>=3.0.0

# MCP server — protocolo universal de integração de agentes (2026)
mcp>=1.27.1

# Observabilidade (opt-in via env vars LANGFUSE_PUBLIC_KEY / LANGFUSE_SECRET_KEY)
langfuse>=3.0.0

# API HTTP — endpoints stateless para os agentes basic / tool / rag
fastapi>=0.136.1
uvicorn[standard]>=0.46.0

# Variáveis de ambiente — pydantic-settings consolida env vars com validação
python-dotenv>=1.2.2
pydantic-settings>=2.14.1

# Utilitários
numpy>=1.26.0
